# 安全策略

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| 1.2.x | ✅ 接受漏洞报告 |
| < 1.2 | ❌ 请先升级 |

## 如何报告漏洞

请使用 GitHub 的**私人漏洞报告**（仓库页 → Security → Report a vulnerability），**不要在公开 Issue 中描述未修复的安全问题**。我们会在 7 天内回应。

## 安全边界（先读这里，多数疑问在此范围之外）

Bilingual-Term-Extract 是**本地运行的 CLI / agent 技能**，安全模型很简单：

- 零第三方运行时依赖，全部脚本为本机 Node.js 执行；代码中**不存在任何网络请求路径**——
  文档内容、候选术语、术语表只在本地文件与本机进程内存中流转；
- LLM 精筛由 agent 会话完成，不经过本工具的任何代码路径；如你的 agent 需要 API key，
  key 只存在于 agent 的环境变量/配置中，**本仓库不包含、也不应包含任何 API Key、令牌或登录凭据**（CI 中的 gitleaks 会拦截提交）；
- 处理的文档（DOCX/TXT/SRT/TMX）按普通本机文件对待：把敏感文档交给任何本地工具前，
  请自行确认所在机器与输出目录（`--out`）的可信度。

因此：**"文本被上传 / 数据外发到服务器"类的报告，请先对照上述边界确认**——本工具没有可外发的通道。

## 对使用者的建议

- 在受控/合规环境中使用时，输出目录请指向项目认可的路径，产物（术语表）视同业务资料管理；
- 将本仓库作为技能安装时，请从本仓库的 git 历史或 Release 拉取，不要执行来路不明的副本。
